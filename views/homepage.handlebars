<h1>homepage</h1>
<ul>
    {{#each posts}}
        <li>
            {{title}} By: {{user.username}} at {{format_date created_at}}
            <h2 style="font-size: 12px">Updated: {{format_date updated_at}}</h2>
            <p>{{body}}</p>
        </li>
    {{/each}}
</ul>